version: '3.8'

services:
  cv-generator:
    build: .
    container_name: cv-resume-generator
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000

